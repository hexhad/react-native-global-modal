export enum PRIORITY {
  LOW = 0,
  HIGH = 1,
}

export enum ALERT_TYPE {
  ERROR = "ERROR",
  WARNING = "WARNING",
  SUCCESS = "SUCCESS",
  INFO = "INFO",
  NOTICE = "NOTICE",
  QUESTION = "QUESTION",
  LOADING = "LOADING",
  TIP = "TIP",
}